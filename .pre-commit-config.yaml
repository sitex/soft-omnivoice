repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.16.5
    hooks:
      - id: ruff
        args: ["--fix"]
        exclude: ^(docs|examples)/
      - id: ruff-format
        exclude: ^(docs|examples)/